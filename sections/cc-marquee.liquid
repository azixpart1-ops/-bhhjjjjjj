{%- liquid
  assign s = section.settings
-%}

{%- if section.blocks.size > 0 -%}
  {% render 'contrast-override', background_color: s.background_color, section_id: section.id %}

  <div class="cc-marquee color-custom-{{ section.id }}" style="--cc-marquee-duration: {{ s.duration }}s;">
    <div class="cc-marquee__track" data-cc-marquee>
      {%- for block in section.blocks -%}
        <span class="cc-marquee__item" {{ block.shopify_attributes }}>
          {%- if s.show_icons -%}{% render 'cc-icon', icon: block.settings.icon, size: 16 %}{%- endif -%}
          {{ block.settings.text }}
        </span>
      {%- endfor -%}
    </div>
  </div>
{%- endif -%}

{% schema %}
{
  "name": "USP marquee",
  "tag": "section",
  "class": "cc-section-marquee",
  "settings": [
    { "type": "checkbox", "id": "show_icons", "label": "Show icons", "default": true },
    {
      "type": "range",
      "id": "duration",
      "label": "Scroll duration",
      "min": 15,
      "max": 90,
      "step": 5,
      "unit": "s",
      "default": 45
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background",
      "default": "#2f3d34"
    }
  ],
  "blocks": [
    {
      "type": "item",
      "name": "Message",
      "settings": [
        {
          "type": "select",
          "id": "icon",
          "label": "Icon",
          "options": [
            { "value": "truck", "label": "Delivery" },
            { "value": "shield", "label": "Guarantee" },
            { "value": "return", "label": "Returns" },
            { "value": "card", "label": "Payment" },
            { "value": "moon", "label": "Sleep" },
            { "value": "hand", "label": "Handmade" },
            { "value": "leaf", "label": "Sustainable" },
            { "value": "award", "label": "Award" },
            { "value": "sparkle", "label": "Sparkle" }
          ],
          "default": "truck"
        },
        { "type": "text", "id": "text", "label": "Text", "default": "Free UK mainland delivery" }
      ]
    }
  ],
  "presets": [
    {
      "name": "USP marquee",
      "category": "Conversion",
      "blocks": [
        { "type": "item", "settings": { "icon": "truck", "text": "Free delivery to your room of choice" } },
        { "type": "item", "settings": { "icon": "moon", "text": "100-night comfort trial" } },
        { "type": "item", "settings": { "icon": "shield", "text": "10-year frame guarantee" } },
        { "type": "item", "settings": { "icon": "hand", "text": "Hand-finished in Britain" } },
        { "type": "item", "settings": { "icon": "card", "text": "Klarna & Clearpay at checkout" } }
      ]
    }
  ]
}
{% endschema %}
